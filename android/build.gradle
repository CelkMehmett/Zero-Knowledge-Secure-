plugins {
    id 'com.android.library' version '7.0.0' apply false
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

// Note: The Android module may need androidx.biometric dependency in its build.gradle
// Add the following to your module-level build.gradle dependencies section where appropriate:
// implementation 'androidx.biometric:biometric:1.2.0'
